export enum Endpoints {

    GET_CATEGORIES               = '/',
    GET_CATEGORY                 = '/:categoryName',

    GET_QUESTIONS                = '/',
    GET_QUESTION                 = '/:questionId',
}

export enum Routes {
    CATEGORIES_ROUTE             = '/categories',
    QUESTIONS_ROUTE              = '/:categoryName/questions',
}